# numeros = [valores for valores in range(1, 1000001)]
# mini = min(numeros)
# maxi = max(numeros)
# soma = sum(numeros)

# totais = [mini, maxi, soma]
# print(totais)

# tres = [valor for valor in range(3, 31, 3)]
# print(tres)

cubos = [valores**3 for valores in range(1, 11)]
print(cubos)
